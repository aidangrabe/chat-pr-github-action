name: 'Teamwork Chat PR Bot'
description: 'Post messages to Teamwork Chat'
inputs:
  CHAT_HOOK_URL:  
    description: 'The Incoming Hook URL to post the messages to'
    required: true
    default: ''
runs:
  using: 'node12'
  main: 'dist/index.js'
branding:
  icon: 'cast'  
  color: 'blue'